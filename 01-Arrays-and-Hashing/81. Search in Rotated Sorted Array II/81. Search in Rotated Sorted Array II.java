                    start = mid + 1;                 } else {                    end = mid - 1;                 if (target >= nums[start] && target < nums[mid]) {            if (nums[start] <= nums[mid]) {                            end--;                continue;            }                start++;            if (nums[start] == nums[mid] && nums[mid] == nums[end]) {            }                return true;            if (nums[mid] == target) {            int mid = start + (end - start) / 2;        while (start <= end) {        int end = nums.length - 1;        int start = 0;        //removeDuplicate(nums);